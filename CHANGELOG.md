# Changelog

本文件记录每个可回滚版本的用户可见变化、关键修复和验证结果。发布前必须先更新本文件，再运行发布脚本。

## [未发布] - 2026-08-14

### 优化

- 重复出现的相同违规话术按每次独立转录分别拦截、累计和保留证据，不再只记录第一次。
- 商品资料新增“商品合规画像”：保存后立即识别行业和标准类目，并在空闲时由豆包补全合规描述、高风险词、语义风险边界、必要披露和安全介绍方向。
- 商品画像默认采用抖音带货直播间规则，可人工修改；豆包和本地生成结果先作为待复核资料，人工确认后明确风险短语才进入本地高风险快速拦截。
- 实时语义复核新增行业、类目和商品画像前置上下文；食品、保健、医疗、护肤等高风险行业在均衡和优化档位提高抽检频率。
- 主播下一句预测改为原话优先：先根据主播当前说话内容预测承接表达，商品资料只作事实边界；主播明确提及其他商品时，先自动切换商品再发起提词请求。
- 风险卡显示置信度、完整违规原话和命中片段，并支持中控一键确认写入当前直播间本地规则库。
- 新增本地规则到公共规则库的两级治理：商家提交服务运营审核，运营可采纳、待定或舍弃；只有采纳复制到公共库，其他决定不影响原直播间规则。
- 增加独立 `/operations` 运营审核页面，普通商家账号不能执行公共规则采纳。
- 新增持久化“待处置风险”：每次非安全结果都保留到人工确认成规则或标记误判，不再被下一句、刷新、断线、换商品或下播覆盖。
- 资料管理的风险规则控制台分开显示待处置风险、已发布本地规则和可折叠的已处置记录；直播侧保留最近四条待办，避免挤占操作空间。
- 确认规则接口改用服务端保存的 `sessionId + segmentId` 证据，并保存风险发生时商品快照；商品后来切换、修改或移出直播间仍可正确确认原规则。

### 根因与边界

- 旧实时管线会丢弃较早片段晚到的豆包结果，且缓存结果复用同一 finding ID；现改为按 `segmentId` 生成独立 finding。旧片段风险只写入提醒和统计，不覆盖当前风险主卡或提词；同一片段的本地结果被豆包升级时仍按片段替换，不重复累计。
- 旧商品资料只有自由文本分类，无法稳定表达行业特有的功效、资质和披露要求；画像内嵌在商品 JSON 中，豆包只生成建议，不会未经人工确认就自动发布强拦截规则。
- 商品保存不等待远程模型：本地画像立即生效，豆包补全走低优先模型通道，最多占用 1 个并发并为实时审核和提词保留容量；网络或方舟不可用不影响商品录入、收音和本地通用规则。
- 旧提词把商品资料和主播模板排在主播当前原话之前，容易生成脱离现场节奏的商品介绍；本次只调整提词输入优先级和本地兜底，不改变商品资料的事实校验职责。
- 旧风险结果缺少人工确认到公共规则的治理链路；本次新增本地确认、服务运营审核和公共库复制，不自动把模型结果写入公共库，也不让公共审核结果删除商家本地规则。
- 旧页面只让最新风险卡执行确认，新的结果会让未处置风险失去入口；旧确认接口还依赖当前商品并接受页面提交的完整结果。本次将实时提醒与风险处置拆分，待办和处置状态写入 SQLite，页面只提交稳定标识，服务端使用不可被当前页面覆盖的原始证据。

### 验证

- 增加相同违规话术重复出现、豆包缓存结果复用和旧片段语义结果晚到的场次级回归测试。
- 增加商品画像本地分类、豆包结构化输出、离线兜底、人工画像锁定、后台补全、画像词级判断和高风险行业抽检策略测试。
- 增加原话优先提词、自动切品后提词上下文、风险证据高亮、确认规则幂等、句级上下文完整保存和运营审核权限回归测试。
- 增加旧风险持续待办、处置后不被晚到结果重开、前端请求竞态、商品移除后确认、重复确认、重复误判及互斥处置测试。
- 完整单元测试 175 项通过，生产类型检查与构建通过，桌面和移动端同步端到端测试 2 项通过。

### 数据与配置

- 新增 `compliance_findings` 本地投影表，保存每段非安全结果、处置状态和风险发生时商品快照；旧数据库启动时自动补 `product_json` 列，不上传运行数据。
- 新增可选配置 `ARK_PRODUCT_PROFILE_TIMEOUT_MS`，默认 5000ms；仍复用现有方舟 Responses API。
- 公共审核状态继续通过现有规则 JSON、版本表和审计表保存；旧规则继续兼容。
- 多人模式沿用现有 `reviewer` 角色；除商品画像超时外，未新增外部云服务配置。

### 回滚

- 当前改动未发布，回滚到 `v0.3.7`；SQLite 规则版本和审计记录保留，不删除本地数据。

## [0.3.7] - 2026-08-12

### 优化

- 重整直播合规规则架构，支持商品、品类和直播间三级作用域；同一段话按风险等级、作用域、匹配长度和版本号择高处理。
- 豆包高置信词级发现改为当前商品作用域的待审核规则，不再自动上线；新增批准、驳回、停用和回滚接口及审计记录。
- 增加语义复核策略：严格档位逐句复核，均衡档位普通安全表达每 4 句抽样，优化档位每 8 句抽样；医疗、健康和隐喻信号立即复核。
- 语义上下文只保留当前商品最近 12 条主播话术和 30 秒窗口，场外人员内容与过期内容不再污染判断。
- 驳回规则不能通过普通“启用”开关绕过审核，页面改为明确的“重新批准”操作；场外人员提及商品也不会触发自动切品。

### 验证

- 新增语义策略、上下文窗口和规则生命周期回归测试。
- 完整单元测试 153 项通过，生产类型检查、构建以及桌面/移动端同步验收通过。

### 数据与回滚

- 不新增 SQLite 表；继续使用现有 `rule_versions`、`rule_audits` 和资源交付任务表。
- 代码回滚：切换到标签 `v0.3.6`；规则新版本保留在本地数据库，不会覆盖历史版本。

## [0.3.6] - 2026-08-11

### 优化

- 没有主播专属模板话术时，主播“下一句”明确请求豆包大模型根据当前商品事实、主播原话和合规结果独立预测三段可直接照读的话术。
- 有模板时仅作为风格参考，仍要求豆包结合当前商品和风险重新组织，避免误称存在历史模板。
- 豆包生成结果逐段经过本地规则校验，检测到明确风险时剔除并使用本地安全话术补足三段。
- 远程语义复核若把当前原话升级为更高风险，先立即切换到本地安全替代，再让豆包基于新风险重新生成，避免旧提词覆盖复核结论。
- 豆包请求超时或异常时继续显示本地安全三段备选，不让主播屏出现空白。
- 主播屏按实际段数标注豆包生成与本地安全补足，便于中控确认每段建议的来源。

### 验证

- 新增无模板请求载荷、豆包三段返回、失败兜底、生成结果规则校验、语义风险升级重生成和主播屏来源标识回归测试。
- 完整单元测试、生产构建、桌面/移动端端到端验收和差异检查通过。

### 回滚

- 代码回滚：切换到标签 `v0.3.5`。
- 本版本不修改 SQLite 表结构，无需迁移或回滚 `.data-v2`。

## [0.3.5] - 2026-08-11

### 修复

- 增加直播中滚动更新兼容：当前仍运行旧进程时，空商品描述会自动使用兼容内容重试，不需要结束正在进行的直播。
- 新建商品保存成功后，页面主动提交本场商品阵容更新，避免旧进程再次用保存前阵容覆盖新商品。
- 新版后端启动后自动使用原生空描述和新增商品同步逻辑，兼容分支只在旧进程明确返回对应错误时启用。

### 验证

- 新增旧进程兼容重试和前端本场阵容命令回归测试。
- 完整单元测试、生产构建和桌面/移动端商品新建同步验收通过。

### 回滚

- 代码回滚：切换到标签 `v0.3.4`。
- 本版本不修改 SQLite 表结构，无需迁移或回滚 `.data-v2`。

## [0.3.4] - 2026-08-11

### 修复

- 商品资料新增明确的“新增商品”入口，不再只显示难以发现的加号按钮。
- 新建直播间商品时只要求名称、分类、价格和商品编码；描述、卖点和参考话术可保存后继续补充。
- 服务端同步放开空描述和空话术列表，前后端采用一致的最小商品校验标准。
- 修复新商品写入直播间目录后，被旧的本场商品阵容覆盖并从页面消失的问题；新商品现在会追加到正在进行的本场商品阵容。
- 编辑现有商品仍在原位置实时刷新，删除商品仍保持直播间至少有一个商品。

### 验证

- 新增界面最小商品创建、HTTP 保存和本场商品阵容同步回归测试。
- 完整单元测试（137 项）、生产构建以及桌面/移动端新建商品和主播屏同步验收通过。

### 回滚

- 代码回滚：切换到标签 `v0.3.3`。
- 本版本不修改 SQLite 表结构，无需迁移或回滚 `.data-v2`。

## [0.3.3] - 2026-08-11

### 优化

- 重整实时合规判定：同一句命中多条规则时按风险等级和规则优先级择高，不再受数组顺序影响。
- 将“保证/一定”等词收窄为与明确效果结果绑定时才拦截，避免“保证品质”等普通表达被误判。
- 对否定、引用和科普敏感功效的表达降为提醒，仍提示主播避免重复平台敏感词。
- 为合规结果补充风险类别、规则编号和处置动作，明确 `blocked` 只替换当前话术，不暂停收音或结束直播。
- 语义复核上下文按当前商品隔离，减少切品后上一件商品话术造成的误判。
- 自动规则学习仅接收豆包高置信词级结果，避免把本地内置规则重复沉淀到直播间规则库。
- 新增 [直播合规拦截与封控标准](docs/COMPLIANCE_POLICY.md)，记录风险矩阵、档位成本和回滚依据。

### 验证

- 新增多规则择高、医疗表达语境降级、普通保证表达、效果承诺和极限词矩阵测试。
- 完整单元测试（135 项）、生产构建、桌面/移动端端到端验收和差异检查通过。

### 回滚

- 代码回滚：切换到标签 `v0.3.2`。
- 本版本不修改 SQLite 表结构，无需迁移或回滚 `.data-v2`。

## [0.3.2] - 2026-08-11

### 修复

- 医疗功效规则新增“治好”表达，修复“耳聋都可以治好”未被拦截的问题。
- 增加疾病或症状与治疗、治愈、治好、根治、医治之间的双向组合判断，覆盖功效动词出现在疾病词前后的表达。
- 医疗组合规则优先于普通绝对化规则，确保“耳聋能够根治”等表达归类为“医疗功效暗示”。
- 仅提及疾病或适用人群、未作治疗功效承诺时，不触发该组合规则。

### 验证

- 回归覆盖“耳聋都可以治好”“治好耳聋”“耳聋能够根治”和不含治疗承诺的耳聋人士使用场景。
- 完整单元测试、生产构建和桌面/移动端端到端验收通过。

### 回滚

- 代码回滚：切换到标签 `v0.3.1`。
- 本版本不修改数据库结构，无需回滚 `.data-v2`。

## [0.3.1] - 2026-08-11

### 修复

- 操作台打开已结束场次 URL 时，自动进入同直播间的新空闲场次，并同步替换浏览器中的场次 ID。
- 空闲场次明确显示“开启直播录制”，暂停和直播状态统一使用录制文案。
- 成功开启录制后自动折叠收音设备选择菜单；设备申请失败时保留菜单，便于重新选择。
- 服务关闭时先终止所有 WebSocket 客户端，再关闭 HTTP 监听，避免重启后旧页面继续连接旧进程。
- 重复点击结束直播时合并为一次 ASR 排空与一次结束事件，收尾期间按钮显示“正在结束”并禁用。
- 主播话术归档等非关键订阅任务失败时只写错误日志，不再阻塞直播场次进入 `ended`。
- 自动恢复旧场次缺失的主播档案，保证下播话术能够正常归档。

### 验证

- 单元与集成测试覆盖旧场次跳转、WebSocket 关闭、重复结束、归档异常和旧主播档案恢复。
- 生产构建、桌面端和移动端同步验收通过。

### 回滚

- 代码回滚：切换到标签 `v0.3.0`。
- 数据回滚：恢复升级前单独备份的 `.data-v2`；不要使用新版本运行后产生的数据库覆盖旧备份。

## [0.3.0] - 2026-08-10

### 新增

- 本地模块化单体、SQLite/WAL 事实源、双轨音频保存和手动交付流程。
- 实时风险预警、三段主播提词、商品自动切换、多人说话标记和历史复核。
- 商品目录按直播间隔离，直播中商品资料可实时同步到中控台和主播屏。
- 主播屏临时短地址与二维码、收音设备选择和热切换。

### 回滚

- 切换到标签 `pre-architecture-rewrite` 并恢复旧 `.data` 备份。
